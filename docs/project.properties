# The location of the common build system
common.build.dir=${basedir}/../projects/common-build

javac.source=1.3
javac.target=1.3
project.title=SpringModules Documentation

# override docs properties

#
# overwrite doc book properties
#

# (Per module settings) 
# where all project documentation resides (per module)
docs.dir=${basedir}

# where all project reference documentation resides (per module)
#docs.ref.dir=${docs.dir}/reference

# where all project reference documentation sources reside (per module)
#docs.ref.src.dir=${docs.dir}/reference/src

# where the project reference documentation is built
#docs.ref.target.dir=${basedir}/target/docs/reference

# (Per entire project/all modules) settings (one for all modules)

#
#docs.global.dir = ${common.build.dir}/docs
# where all DocBook libraries reside 
#docs.global.lib.dir=${docs.global.dir}/lib
#docs.global.images.dir=${docs.global.dir}/src/images
#docs.global.styles.dir=${docs.global.dir}/styles

