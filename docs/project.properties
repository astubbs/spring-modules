#
# Properties file for generating the project documentation
#
# Uses defaults as much as possible

# The location of the common build system
common.build.dir=${basedir}/../projects/common-build

javac.source=1.3
javac.target=1.3
project.title=SpringModules Documentation

# override docs properties

#
# overwrite doc book properties
#

# remove the docs since we are already inside the docs folder
docs.dir=${basedir}

